# Howdy FIDO2 配置文件

# 设备策略: 当检测到其他 FIDO2 设备时的行为
# - always_respond: 总是响应，忽略其他设备的存在
# - fallback: 如果有其他 FIDO2 设备则回避，否则响应（默认）
device_policy = fallback

# PAM 服务名称
pam_service = howdy-fido2

# 用户验证缓存超时时间（秒）
verification_timeout = 30

# 是否启用调试日志
debug_logging = false
